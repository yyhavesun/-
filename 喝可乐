# my-code
#include<stdio.h>
int main()
{
	int n = 0;
	scanf("%d", &n);
	int empty = 0;
	int total = 0;
	total = n;
	empty = total;
	while (empty > 1)
	{
		total += empty / 2 ;
		empty=empty / 2 + empty % 2;
	}
	printf("%d", total);
	return 0;
}
